# Provisional continuation record

This directory was created before the original MRM-01 protocol folder became
available in the new app workspace. It is preserved to avoid deleting or
rewriting evidence.

The canonical append-only event ledger is now:

`MRM-01-Protokolle/events.jsonl`

The two records in this directory are superseded by the reconciliation and
domain-verification events in the canonical ledger. Do not append new project
events here.
